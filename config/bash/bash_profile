# ~/.bash_profile: login configuration for Homebrew Bash on macOS.

if [[ -x /opt/homebrew/bin/brew ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

[[ -r "$HOME/.bashrc" ]] && source "$HOME/.bashrc"
